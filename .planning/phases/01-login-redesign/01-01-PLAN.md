---
phase: 01-login-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/src/components/ui/auth-page.tsx
  - apps/dashboard/src/components/ui/button.tsx
  - apps/dashboard/src/components/ui/input.tsx
  - apps/dashboard/src/app/[locale]/(public)/login/page.tsx
  - apps/dashboard/package.json
autonomous: false
---

<objective>
Substituir a página de login atual do dashboard por um novo design baseado no componente auth-page do 21st.dev, mantendo toda a funcionalidade de autenticação Supabase existente.

Purpose: Modernizar a interface de login com um design mais atraente e profissional, sem alterar a lógica de autenticação backend.
Output: Nova página de login funcional com o design auth-page integrado ao fluxo de autenticação existente.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
O projeto Mid Poker usa:
- Next.js 16 com App Router
- Autenticação via Supabase (email/password)
- Componente EmailSignIn existente em apps/dashboard/src/components/email-sign-in.tsx
- Tailwind CSS configurado
- Framer Motion já instalado
- Estrutura de monorepo com Turborepo + Bun

@apps/dashboard/src/app/[locale]/(public)/login/page.tsx
@apps/dashboard/src/components/email-sign-in.tsx

O backend de autenticação NÃO deve ser alterado. Apenas o frontend será substituído.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar dependências necessárias</name>
  <files>apps/dashboard/package.json</files>
  <action>
    Instalar as dependências npm necessárias para o componente auth-page:
    - lucide-react (ícones)
    - @radix-ui/react-slot (componente Button)
    - class-variance-authority (variantes do Button)

    Executar: `cd apps/dashboard && bun add lucide-react @radix-ui/react-slot class-variance-authority`

    Nota: framer-motion já está instalado (verificado no package.json).
  </action>
  <verify>
    - bun pm ls | grep lucide-react confirma instalação
    - bun pm ls | grep @radix-ui/react-slot confirma instalação
    - bun pm ls | grep class-variance-authority confirma instalação
  </verify>
  <done>Dependências instaladas e verificadas no package.json</done>
</task>

<task type="auto">
  <name>Task 2: Criar estrutura de componentes shadcn/ui</name>
  <files>
    apps/dashboard/src/components/ui/button.tsx,
    apps/dashboard/src/components/ui/input.tsx,
    apps/dashboard/src/lib/utils.ts
  </files>
  <action>
    1. Criar diretório apps/dashboard/src/components/ui/ (se não existir)

    2. Criar apps/dashboard/src/lib/utils.ts com função cn (class names helper):
    ```typescript
    import { type ClassValue, clsx } from "clsx"
    import { twMerge } from "tailwind-merge"

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    3. Copiar o componente Button fornecido para apps/dashboard/src/components/ui/button.tsx (código completo fornecido pelo usuário)

    4. Copiar o componente Input fornecido para apps/dashboard/src/components/ui/input.tsx (código completo fornecido pelo usuário)

    Importante: Verificar se clsx e tailwind-merge já estão instalados. Se não, adicionar ao package.json.
  </action>
  <verify>
    - ls apps/dashboard/src/components/ui/ mostra button.tsx e input.tsx
    - ls apps/dashboard/src/lib/utils.ts existe
    - tsc --noEmit (no contexto do dashboard) não mostra erros nesses arquivos
  </verify>
  <done>Componentes base shadcn/ui criados e compilando sem erros</done>
</task>

<task type="auto">
  <name>Task 3: Adaptar componente AuthPage com autenticação Supabase</name>
  <files>apps/dashboard/src/components/ui/auth-page.tsx</files>
  <action>
    1. Criar apps/dashboard/src/components/ui/auth-page.tsx baseado no código fornecido

    2. ADAPTAÇÕES CRÍTICAS para integrar com Supabase:

    a) Remover botões sociais (Google, Apple, GitHub) - o Mid Poker usa apenas email/password

    b) Substituir o formulário de email estático por integração com o componente EmailSignIn existente:
    ```typescript
    import { EmailSignIn } from "@/components/email-sign-in";
    ```

    c) Manter a estrutura visual (grid duas colunas, FloatingPaths, etc.)

    d) Adaptar textos:
    - Título: "Sign In or Join Now!" → "Welcome to Mid Poker"
    - Descrição: ajustar para "Sign in to your account to continue"
    - Remover referência "Asme" e usar "Mid Poker"

    e) Substituir o ícone Grid2x2PlusIcon por Icons.LogoSmall do pacote @midpoker/ui/icons

    f) Manter links de Terms of Service e Privacy Policy apontando para:
    - Terms: https://mid.poker/terms
    - Privacy: https://mid.poker/policy

    g) Remover o separador "OR" e botões sociais - manter apenas o formulário de email

    NÃO modificar a lógica de autenticação do EmailSignIn. Apenas usar o componente como está.
  </action>
  <verify>
    - cat apps/dashboard/src/components/ui/auth-page.tsx mostra código adaptado
    - grep "EmailSignIn" apps/dashboard/src/components/ui/auth-page.tsx confirma integração
    - grep "Supabase" apps/dashboard/src/components/email-sign-in.tsx confirma que lógica original está intacta
    - tsc --noEmit não mostra erros no componente
  </verify>
  <done>Componente AuthPage criado com integração Supabase via EmailSignIn</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar página de login para usar novo componente</name>
  <files>apps/dashboard/src/app/[locale]/(public)/login/page.tsx</files>
  <action>
    Substituir completamente o conteúdo de page.tsx para usar o novo AuthPage:

    ```typescript
    import { AuthPage } from "@/components/ui/auth-page";
    import type { Metadata } from "next";

    export const metadata: Metadata = {
      title: "Login | Mid Poker",
    };

    export default async function Page() {
      return <AuthPage />;
    }
    ```

    Remover todas as referências antigas a:
    - LoginTestimonials
    - Vídeo de fundo
    - Layout anterior

    A nova página AuthPage é autossuficiente e contém todo o layout necessário.
  </action>
  <verify>
    - cat apps/dashboard/src/app/[locale]/(public)/login/page.tsx mostra código simplificado
    - grep "AuthPage" apps/dashboard/src/app/[locale]/(public)/login/page.tsx confirma import
    - grep -v "LoginTestimonials\|video" confirma remoção de código antigo
  </verify>
  <done>Página de login atualizada para usar AuthPage</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Nova página de login com design auth-page integrado à autenticação Supabase</what-built>
  <how-to-verify>
    1. Iniciar servidor de desenvolvimento:
       ```bash
       cd apps/dashboard
       bun run dev
       ```

    2. Navegar para: http://localhost:9000/login (ou porta configurada)

    3. Verificar design visual:
       - Desktop (>1024px): Layout com 2 colunas, animações FloatingPaths visíveis
       - Tablet/Mobile: Layout responsivo, coluna única
       - Logo Mid Poker no topo esquerdo
       - Formulário de login centralizado na direita

    4. Testar funcionalidade de autenticação:
       - Inserir email e senha de teste
       - Clicar em "Sign in with Email"
       - Verificar que autenticação Supabase funciona (redirect após login)
       - Testar modo "Sign up" (criar conta)
       - Verificar mensagens de erro (email inválido, senha errada)

    5. Verificar elementos visuais:
       - Animações dos paths de fundo funcionam suavemente
       - Links de Terms of Service e Privacy Policy funcionam
       - Responsividade em diferentes tamanhos de tela
       - Sem erros no console do browser

    6. Confirmar que o BACKEND não foi alterado:
       - Autenticação ainda usa Supabase
       - Cookies de sessão são criados corretamente
       - Redirect após login funciona
  </how-to-verify>
  <resume-signal>Digite "approved" se tudo funciona corretamente, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
Antes de declarar o plano completo:
- [ ] `bun run build` no dashboard compila sem erros
- [ ] `bun run typecheck` no dashboard não mostra erros TypeScript
- [ ] Página de login carrega e renderiza corretamente
- [ ] Autenticação Supabase funciona (email/password)
- [ ] Layout responsivo funciona em mobile, tablet e desktop
- [ ] Animações de background funcionam suavemente
- [ ] Nenhum erro no console do navegador
</verification>

<success_criteria>

- Todas as tasks completadas
- Todas as verificações passam
- Nova página de login visualmente implementada
- Autenticação Supabase preservada e funcional
- Nenhum erro TypeScript ou de build
- Interface responsiva em todos os tamanhos de tela
- Usuário aprovou visualmente o resultado
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-login-redesign/01-01-SUMMARY.md` documentando:
- Componentes criados (auth-page.tsx, button.tsx, input.tsx)
- Dependências instaladas
- Adaptações feitas para integrar Supabase
- Screenshots ou descrição da nova interface
- Confirmação de que autenticação backend não foi alterada
</output>
