---
phase: 01-mapeamento-fluxo-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md]
autonomous: true
---

<objective>
Mapear completamente o fluxo backend do módulo poker, desde os routers tRPC até as queries do banco de dados.

Purpose: Criar documentação técnica do backend, identificando todos os routers tRPC, schemas de validação Zod, queries Drizzle, e lógica de negócio envolvida na gestão de clubes, importação, validação e settlements.

Output: Documento `.planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md` com mapeamento completo do backend.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

# Contexto adicional do CLAUDE.md
Poker module backend está em:
- Routers tRPC: apps/api/src/trpc/routers/poker/
- Schemas Zod: apps/api/src/schemas/poker/
- Database queries: packages/db/src/queries/ (43 arquivos)
- Database schema: packages/db/src/schema.ts (entidades poker_*)

Routers principais:
- players.ts, sessions.ts, transactions.ts, settlements.ts, analytics.ts, poker-import.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mapear routers tRPC do poker module</name>
  <files>.planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md</files>
  <action>
    Explorar e documentar:

    1. **Identificar todos os routers tRPC do poker**:
       - Listar routers em apps/api/src/trpc/routers/poker/
       - Confirmar os 6 routers principais: players, sessions, transactions, settlements, analytics, poker-import
       - Verificar se há routers adicionais

    2. **Para cada router, documentar**:
       - **poker-import.ts**: Procedures de upload, validação, processamento de planilhas
       - **players.ts**: CRUD de jogadores/agentes, hierarquia (agent_id, super_agent_id)
       - **sessions.ts**: Gestão de sessões de poker (cash_game, mtt, sit_n_go, spin)
       - **transactions.ts**: Operações com transações de chips (12 tipos)
       - **settlements.ts**: Lógica de fechamento semanal, cálculos, status tracking
       - **analytics.ts**: Rake, bank result, top players, relatórios

    3. **Documentar procedures**:
       - Listar procedures públicas em cada router (list, get, create, update, delete, etc.)
       - Identificar inputs (Zod schemas) e outputs (tipos de retorno)
       - Mapear middleware utilizado (rate limiting, team permission, read-after-write)
       - Identificar procedures críticas (validação, processamento, settlement)

    Criar documento estruturado com:
    - Seção para cada router com lista de procedures
    - Inputs/outputs de cada procedure
    - Middleware chain aplicado
    - Procedures críticas destacadas
  </action>
  <verify>
    - cat .planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md contém seção "Routers tRPC"
    - Documento lista os 6 routers principais do poker
    - Procedures de cada router estão documentadas
  </verify>
  <done>
    - Todos os routers tRPC do poker identificados
    - Procedures de cada router listadas com inputs/outputs
    - Middleware chain documentado
    - Procedures críticas destacadas (validação, settlement)
  </done>
</task>

<task type="auto">
  <name>Task 2: Mapear schemas de validação Zod e queries do banco</name>
  <files>.planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md</files>
  <action>
    Continuar documentação identificando:

    1. **Schemas de validação (Zod)**:
       - Buscar em apps/api/src/schemas/poker/ schemas relacionados ao poker module
       - Identificar schema de importação (validação das 12+ regras)
       - Documentar schemas de players, sessions, transactions, settlements
       - Mapear regras de validação críticas (estrutura, IDs, balanço, totais)

    2. **Database queries (Drizzle)**:
       - Buscar em packages/db/src/queries/ queries relacionadas a poker
       - Identificar queries reutilizáveis: getPlayerById, listSessionsByTeam, etc.
       - Documentar queries complexas (joins, agregações)
       - Mapear queries críticas para settlements e validação

    3. **Database schema**:
       - Ler packages/db/src/schema.ts e extrair entidades poker_*
       - Documentar: poker_players, poker_sessions, poker_session_players, poker_chip_transactions, poker_settlements, poker_imports
       - Mapear relacionamentos (foreign keys, índices)
       - Identificar campos críticos para auditoria (status, amounts, timestamps)

    Adicionar ao documento:
    - Seção "Schemas de Validação" com lista de schemas Zod e regras principais
    - Seção "Database Queries" com queries reutilizáveis por domínio
    - Seção "Database Schema" com entidades e relacionamentos
    - Diagrama de relacionamentos (texto/ASCII)
  </action>
  <verify>
    - Documento contém seções "Schemas", "Queries", "Database Schema"
    - Pelo menos 6 entidades poker_* identificadas
    - Regras de validação das 12+ regras documentadas
  </verify>
  <done>
    - Schemas Zod mapeados com regras de validação
    - Queries Drizzle identificadas e categorizadas
    - Entidades do banco documentadas com relacionamentos
    - Campos críticos para auditoria destacados
  </done>
</task>

<task type="auto">
  <name>Task 3: Consolidar mapeamento backend em documento final</name>
  <files>.planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md</files>
  <action>
    Revisar e consolidar o documento criado:

    1. **Adicionar visão geral**:
       - Sumário executivo do backend poker
       - Tecnologias utilizadas (Hono, tRPC, Drizzle ORM, PostgreSQL, Zod)
       - Arquitetura geral (middleware chain, context creation, query patterns)

    2. **Mapear fluxo de dados**:
       - Documentar fluxo: tRPC request → middleware → procedure → query → database → response
       - Identificar pontos de transformação de dados
       - Mapear validações em cada camada (Zod input, database constraints)

    3. **Completar gaps**:
       - Verificar se todos os aspectos críticos estão cobertos:
         - Sistema de importação completo (upload → parsing → validação → processamento)
         - Lógica de settlements (cálculos → status tracking → finalização)
         - Gestão de transações (12 tipos, balanço, atomicidade)
       - Adicionar observações sobre tech debt encontrado

    4. **Identificar pontos críticos para auditoria**:
       - Lógica de validação das 12+ regras (onde está, como funciona)
       - Cálculos matemáticos de settlements (precisão, edge cases)
       - Transações atômicas e consistência de dados
       - Queries complexas que precisam de revisão

    5. **Estruturar documento**:
       - Organizar em seções claras
       - Adicionar índice no início
       - Incluir referências a arquivos e linhas específicas
       - Criar lista de próximos passos para Fase 2
  </action>
  <verify>
    - wc -l .planning/phases/01-mapeamento-fluxo-ux/01-02-BACKEND-MAP.md mostra pelo menos 100 linhas
    - Documento tem índice e está bem estruturado
    - Fluxo de dados completo está documentado
    - Pontos críticos identificados para auditoria
  </verify>
  <done>
    - Documento final consolidado e revisado
    - Visão geral adicionada com sumário executivo
    - Fluxo de dados completo mapeado (request → database → response)
    - Pontos críticos identificados: validação, settlements, transações
    - Referências a arquivos específicos incluídas
    - Lista de áreas para auditoria profunda na Fase 2
  </done>
</task>

</tasks>

<verification>
Antes de declarar o plano completo:
- [ ] Documento 01-02-BACKEND-MAP.md existe e está completo
- [ ] Todos os routers tRPC do poker identificados e documentados
- [ ] Schemas de validação Zod mapeados
- [ ] Database queries e schema documentados
- [ ] Fluxo de dados completo mapeado
- [ ] Pontos críticos identificados para próximas fases
</verification>

<success_criteria>

- Documento de mapeamento backend criado com pelo menos 100 linhas
- 6 routers tRPC principais documentados com procedures
- Schemas Zod listados com regras de validação
- Database schema com entidades poker_* e relacionamentos
- Fluxo de dados mapeado: tRPC → middleware → query → database
- Pontos críticos destacados: validação (12+ regras), settlements (cálculos), transações (atomicidade)
- Lista de áreas para auditoria profunda preparada para Fase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-mapeamento-fluxo-ux/01-02-SUMMARY.md`
</output>
